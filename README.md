# CutPaste-Replication
CutPaste-Replication implements the self-supervised CutPaste framework for anomaly detection, reproducing the original patch-based augmentation and classification pipeline. It provides a reference implementation for theoretical evaluation without requiring training or external test data.
